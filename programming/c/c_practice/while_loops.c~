#include <stdio.h>

int main(){

  int n = 0;
  while(n < 10){
    n++;

    if(n%2 == 1){
      continue;
      //go back to the start of the while loop
    }

    printf("The number %d is even.\n", n);
  }

  return 0;
}
